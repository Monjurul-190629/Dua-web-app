[phases.setup]
nixPkgs = ['python3', 'gcc', 'make', 'nodejs', 'pkg-config', 'sqlite']

[phases.install]
cmds = ['npm install']

[phases.build]
cmds = ['npm rebuild better-sqlite3 --build-from-source']